name:
  majiq

channels:
  - bioconda
  - defaults

dependencies:
  - python >= 3.5
  - pip:
      - whell
      - setuptools
      - cython
      - pysam
      - numpy
      - git+https://bitbucket.org/biociphers/majiq_stable.git#egg=majiq